def absolute(num: float) -> float:
    """
    Calculate the absolute
    :param num:
    :return:
    """
    if num < 0:
        return -num
    return num

print(absolute(-5))

help(absolute)

help(print)
