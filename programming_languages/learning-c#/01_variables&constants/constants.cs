using System;

public class Program
{
    static void Main()
    {
        const int ConstValue = 10; // Compile-time constant
        // readonly int ReadOnlyValue; // Run-time constant
        
        // ReadOnlyValue = 43;

        Console.WriteLine(ConstValue);
    }
}
